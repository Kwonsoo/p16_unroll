(* Stuffs related to alarm reduction *)

(*
open Types

module Reducer : sig

	(* Reduce the programs in the T1 set. *)
	val reduce : dir -> dir -> unit

end = struct

	let reduce = fun t1dir rdir -> prerr_endline ">> Raw feature code has been reduced."; ()

end
*)
