/*
 * Bot 라서 diff 에서 사라져주어야 하는데 diff에 있는 알람에 대한 예제
 */

int main() {
	char *tmp;
	char *t;
	int d;

	tmp = memchr();
	t = tmp;
  airac_print (t);
	t += d;		// offset 이 bot 가 되는 이유. airac_observe FS offset값이 bot 가 됨.
  airac_print (t);
	airac_observe(t, -2);
	if (*(t + -2) == 'y') {	// dereference 지점.
		func();
	}
}
